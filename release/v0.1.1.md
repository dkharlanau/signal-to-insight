# Signal to Insight v0.1.1

This documentation patch puts a safe, runnable proof of the released public walkthrough near the top of the README.

## Highlights

- One copy-paste command validates the walkthrough, public routes, published explainer, and protected review previews.
- The expected validation result is stated explicitly.
- The public walkthrough and learning/adoption boundary are visible before the detailed product model.

## Verify

Run:

```bash
python scripts/validate_public_surface.py
python sti.py validate
```

The release attaches a deterministic source archive and checksums built from the tagged commit.

## Compatibility and non-goals

This patch does not change schemas, source/insight data, generated knowledge surfaces, publication state, or the v0.1 lifecycle contract. Validation success is not a human learning result or evidence of external adoption.
